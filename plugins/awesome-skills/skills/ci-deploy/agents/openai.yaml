interface:
  display_name: "CI Deploy"
  short_description: "Approve a CircleCI gate and watch the deploy."
  default_prompt: "Deploy the current project using $ci-deploy. With no environment named, deploy the sole environment automatically, or ask which one if several exist."

dependencies:
  tools: []
