interface:
  display_name: "Vault Share"
  short_description: "Privately DM a Vault secret via Slack."
  default_prompt: "Use $vault-share to automatically handle the latest DB credential request from #database-credentials-ops. If explicit target and recipient input is provided, use that instead."

dependencies:
  tools:
    - type: "mcp"
      value: "mcp-vault"
      description: "Vault MCP server"
